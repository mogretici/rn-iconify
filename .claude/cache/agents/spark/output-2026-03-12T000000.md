# Quick Fix: Accessibility Doc Updates

Generated: 2026-03-12T00:00:00

## Changes Made

### 1. Replace grayscale API with invertColors API

- File: `docs/docs/features/accessibility.mdx`
- Lines: 175-182 (after edit)
- Change: Replaced `isGrayscaleEnabled()` with `isInvertColorsEnabled()` and event `'grayscaleChanged'` with `'invertColorsChanged'`. Updated inline comment to remove mention of `isGrayscaleEnabled`.

### 2. isBoldTextEnabled as high-contrast proxy

- No reference to `isBoldTextEnabled` was found in the file. No change needed for this item.

### 3. Add v3.0 Automatic Accessibility section

- File: `docs/docs/features/accessibility.mdx`
- Lines: 11-19 (after edit)
- Change: Inserted `### Automatic Accessibility (v3.0)` section between the intro paragraph and `## Accessibility Labels`.

## Verification

- Old terms (`isGrayscaleEnabled`, `grayscaleChanged`, `isBoldTextEnabled`) confirmed absent via tldr search.
- New terms (`isInvertColorsEnabled`, `invertColorsChanged`, `Automatic Accessibility`) confirmed present via file read.

## Files Modified

1. `docs/docs/features/accessibility.mdx` - Three targeted edits as described above.

## Notes

- No `isBoldTextEnabled` reference existed in the file, so fix #2 had nothing to change.
- The v3.0 section uses `###` (h3) to sit visually below the page intro without competing with the `##` (h2) section headers that follow.
